{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build WebAssembly",
            "type": "shell",
            "command": "emcc main.cpp -o index.html -s USE_WEBGL2=1",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
